import { Pressable, View, Text, StyleSheet, Image } from "react-native";

const SearchTabType = ({ navigation }) => {
    return (
      <View style={{ flex: 1, alignItems: 'center', justifyContent: 'space-around' }}>
        <Text>{"Type"}</Text>
      </View>
    );
  }

export default SearchTabType;